type: object
required:
  - code
  - language
properties:
  environment:
    $ref: ./ExecutionEnvironmentSpec.yml
  code:
    type: string
  language:
    type: string
  max_retries:
    type: integer
    format: int4
    default: 5
  timeout:
    type: integer
    format: int32
    default: -1
