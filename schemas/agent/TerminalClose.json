{
    "description": "Close terminal session",
    "properties": {
        "terminalId": {
            "description": "Unique identifier for the terminal session",
            "format": "uuid",
            "title": "Terminalid",
            "type": "string"
        },
        "msgType": {
            "default": "TerminalClose",
            "title": "Msgtype",
            "type": "string"
        }
    },
    "required": [
        "terminalId"
    ],
    "title": "TerminalClose",
    "type": "object"
}
