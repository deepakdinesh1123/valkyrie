{
    "description": "Represents a request to delete a directory in a sandbox.",
    "properties": {
        "msgType": {
            "default": "DeleteDirectory",
            "description": "Type of the message",
            "title": "Msgtype",
            "type": "string"
        },
        "path": {
            "description": "Path of the directory to be deleted",
            "title": "Path",
            "type": "string"
        }
    },
    "required": [
        "path"
    ],
    "title": "DeleteDirectory",
    "type": "object"
}
