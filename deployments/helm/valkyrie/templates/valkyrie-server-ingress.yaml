apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: valkyrie-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: valkyrie-service
                port:
                  name: http
