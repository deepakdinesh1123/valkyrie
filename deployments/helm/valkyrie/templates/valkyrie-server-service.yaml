apiVersion: v1
kind: Service
metadata:
  name: valkyrie-service
  labels:
    app: valkyrie
spec:
  selector:
    component: server
    app: valkyrie
  ports:
    - name: web
      port: 80
      targetPort: web
